import { StyleService } from "@ui-kitten/components";

export const loadingStyle = StyleService.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
})