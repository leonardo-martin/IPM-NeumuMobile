import { StyleSheet } from 'react-native'

export const headerStyle = StyleSheet.create({
  layout: {
    paddingTop: 30,
    backgroundColor: '#FAFAFA'
  },
  container: {
    backgroundColor: '#FAFAFA'
  }
})
