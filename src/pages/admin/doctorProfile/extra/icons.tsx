import React from "react"
import { Icon, IconElement, IconProps } from "@ui-kitten/components"

export const PhoneCallIcon = (props: IconProps): IconElement => (
    <Icon {...props} name='phone-call-outline' pack='eva' />
)

export const ShareIcon = (props: IconProps): IconElement => (
    <Icon {...props} name='share-outline' pack='eva' />
)